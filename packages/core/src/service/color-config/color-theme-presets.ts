import type { ColorConfig } from './types'

export const colorsPresets: Record<string, ColorConfig> = {
  default: {
    // Accent
    primary: '#154ec1',
    secondary: '#767C88',
    success: '#40e583'
  }
}
